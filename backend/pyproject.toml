[project]
name = "tenderflow-backend"
version = "0.1.0"
description = "TenderFlow FastAPI + LangGraph backend"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.1",
  "httpx>=0.27.0",
  "pydantic>=2.7.0",
  "python-dotenv>=1.0.1",
]
